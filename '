import { useState } from "react";
import { FormProvider, useForm } from "react-hook-form";
import FileUpload from "../courseInfo/FileUpload";
import { IoMdClose } from "react-icons/io";
export default function LectureForm() {
  const [hours, setHours] = useState("");
  const [sec, setSec] = useState("");
  const [min, setMin] = useState("");
  const methods = useForm({
    defaultValues: {
      title: "",
      description: "",
      lecture: "",
    },
  });
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = methods;
  function submit(data) {
    console.log(data);
  }
  return (
    <div
      className={
        "absolute  top-0 left-0 z-50 bg-custom-tertiary bg-opacity-80 drop-shadow-md flex justify-center w-full h-full"
      }
    >
      <FormProvider {...methods}>
        <form
          onSubmit={handleSubmit(submit)}
          className="rounded-md w-9/12 mt-8"
        >
          <div
            className={
              "bg-custom-secondary rounded-md  flex justify-between w-full px-4 py-4 text-white font-bold "
            }
          >
            <h3 className="text-2xl">Editing Lecture</h3>
            <span>
              <IoMdClose size={24} />
            </span>
          </div>
          <div>
            <label htmlFor="lecture">
              Lecture <span className="text-red">*</span>
            </label>
          </div>
        </form>
      </FormProvider>
    </div>
  );
}
